default_context:
    project_name: "{{ cookiecutter.project_name }}"
    app_name: "{{ cookiecutter.app_name }}"
    use_postgres: "{{ cookiecutter.use_postgres }}"
    use_pgadmin: "{{ cookiecutter.use_pgadmin }}"
    _admin_user_username: "{{ cookiecutter._admin_user_username }}"
    _admin_user_email: "{{ cookiecutter._admin_user_email }}"
    _admin_user_password: "{{ cookiecutter._admin_user_password }}"
    _use_celery: "{{ cookiecutter._use_celery }}"
    _wsgi_server: "{{ cookiecutter._wsgi_server }}"
    _python_version: "{{ cookiecutter._python_version }}"
    _tox_python_env: "{{  cookiecutter._tox_python_env}}"
    _copy_without_render: "{{ cookiecutter._copy_without_render }}"
